[package]
name = "sqlite-readiness-probe"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[workspace]

[dependencies]
rusqlite = { version = "=0.40.2", default-features = false, features = ["bundled"] }
